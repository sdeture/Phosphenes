# Phosphenes — desktop viewer and analysis scripts.
#
# The WEB viewer needs none of this: it is static files plus a local HTTP server
# (python3 -m http.server), and Python's stdlib provides that.
#
# transformers>=5.0 is a hard floor, not a preference: with huggingface-hub>=1.0
# installed, older transformers fails to load the tokenizer *silently*, and the
# consequence is a viewer whose text panel and turn markers are simply absent
# with no error.

numpy>=1.24
scipy>=1.10
pygame>=2.5
scikit-learn>=1.3
transformers>=5.0
Pillow>=10.0          # analysis/make_figures.py
matplotlib>=3.7       # analysis/make_figures.py
